# MacParakeet v0.7.3 Telemetry Health Audit

**Snapshot:** 2026-07-17, approximately 10:20 PDT

**Release:** v0.7.3, published 2026-07-17 01:44:51 UTC

**Verdict:** Keep v0.7.3 deployed under an elevated `WATCH`; the evidence does
not support an emergency rollback or speculative app hotfix. Historical stats
rollups are healthy, raw deletion remains disabled, and the crash signal is
neither a one-off nor yet attributable to a v0.7.3 call site.

## Executive verdict

- **No rollback or emergency app hotfix is indicated.** In the first roughly
  16 hours, v0.7.3 had 181 GUI sessions, 683-689 dictation starts (the count
  advanced during the audit), 109 transcription starts, and one crashing
  session: 99.45% crash-free.
- **The crash count is one on v0.7.3, but the signature is not one-off.** The
  same normalized 11-frame SIGSEGV signature appeared four times on v0.7.2 and
  once on v0.7.3, always on macOS 26.5.2. The evidence points toward a recurring
  SQLite/CoreData/SwiftUI framework-side family, not a new v0.7.3 regression.
  The initiating app operation cannot be established from the available custom
  crash payload.
- **Raw-event retention is not operating.** The retention Worker is present in
  source but is absent from the live Cloudflare account. Production contains
  62,943 raw events older than the documented 90-day limit.
- **The stats path is too scan-heavy.** D1 reported 5.788 billion rows read in
  the preceding 24 hours. If that day were representative for a 30-day month,
  paid D1 row-read overage would be approximately **$149/month**. One uncached
  `/api/stats` request returned HTTP 500; three subsequent requests succeeded.
  The expensive five-minute snapshot regeneration is the leading hypothesis,
  not a proven root cause of that single 500.
- **The deterministic reviewer has a surface-classification bug.** It maps
  `surface IS NULL` Sparkle checks to GUI and groups by the underlying column,
  producing duplicate GUI rows and inflating GUI exposure in its report. The
  public stats endpoint correctly uses `surface = 'gui'` and is not affected by
  this specific bug.

## Production follow-up: 2026-07-17 to 2026-07-18

The standard no-deletion remediation described by this audit is now live:

- Website pull request
  [#28](https://github.com/moona3k/macparakeet-website/pull/28) merged as
  `34303079ff58101a8e6a100c20dd09d97c762f46` after the final Greptile and
  CodeRabbit gates passed.
- The rollout's Cloudflare Pages deployment was
  `28093708-ed6e-4b73-a470-6c3dc88c7b5c`, built from the merged source.
  Scheduled rollup Worker version
  `75478242-0f98-4bfb-bcaf-4dd2e3f99664` runs the seven-day daily refresh at
  01:30 UTC and the 90-day Sunday reconciliation at 03:00 UTC.
- The production backfill ran in bounded chunks and was rerun to prove
  idempotency. Raw-versus-rollup verification returned zero deltas for
  all-time totals, every daily bucket, overall speed, chip speed, and app
  category dimensions before and after a scheduled refresh.
- On 2026-07-18, a fresh production parity run again returned zero deltas. The
  state was `ready`, backfilled from 2026-03-13, rolled through 2026-07-17,
  carried no error, and still had `delete_safe_through = NULL`.
- The public `/api/stats` endpoint returned HTTP 200 with
  `history.source = "rollup"`, 128 daily buckets, and the expected cache and
  stale-while-revalidate policy. The `/stats` JSON contract is unchanged.
- A 758 MB pre-migration SQL export is stored at
  `/Users/dmoon/Desktop/macparakeet-telemetry-pre-rollups-2026-07-17.sql` with
  SHA-256
  `c596c690c55beaf56c2dd2b09c4b345b09a1b216b0d365d6645f20f542767896`.
  Cloudflare Time Travel also resolves the pre-migration timestamp to bookmark
  `00003495-00003996-000050ab-17914e121c0d90786510bc39fdbe30b0`.
  The local file is restricted to owner-only mode `0600` and the Mac reports
  FileVault enabled; the permission change did not alter the digest.
- On 2026-07-18, that full export was restored into a disposable SQLite
  database. `PRAGMA integrity_check` returned `ok`; the restore contained
  1,744,713 events with the expected timestamp bounds, snapshot, and five event
  indexes, and no post-migration rollup tables. The disposable restore was
  removed after verification.
- No raw events were deleted, no deletion-safe watermark was set, and the
  retention Worker remains undeployed. Future deletion still requires a new
  explicit authorization, fresh recovery artifacts, sustained rollup health,
  monitoring, and post-delete parity verification.

This follow-up closes the scan-heavy stats remediation only. It deliberately
does not close or silently perform the separate raw-retention rollout.

## Crash-health recheck: 2026-07-18

A final live check found that the public 24-hour chart had advanced from one to
ten v0.7.3 crash-reporting sessions. That number was reproducible but combined
real native incidents with two telemetry-accounting defects:

- pending crash files are uploaded by a new relaunch session, and one native
  incident was uploaded three times under three reporting sessions; and
- the chart grouped by the reporting row's `app_ver` instead of the payload's
  `crash_app_ver`, allowing an older crash to be assigned to a newer build.

For the fixed public snapshot ending at `2026-07-18T21:35:11.091Z`, the honest
result is 8 distinct v0.7.3 crash incidents across 225 observed GUI sessions,
not 10 independently crashing sessions. Across equal first-43-hour-50-minute
release-age windows, v0.7.3 had 9 distinct incidents across 401 sessions
(approximately 97.76% crash-free) versus 5 across 579 sessions for v0.7.2
(approximately 99.14%). The difference warrants monitoring but remains a small
sample rather than proof of regression.

The v0.7.3 incidents separate into:

- 6 Sonoma SIGBUS incidents in two short crash loops: four on macOS 14.7.7 and
  two on macOS 14.4. Similar normalized SIGBUS families occur on v0.7.2 and
  older builds.
- 4 uploaded macOS 26.5.2 SIGSEGV reports representing 2 distinct incidents.
  The normalized 11-frame family matches v0.7.2; one incident accounts for the
  three duplicate uploads.
- 1 macOS 27.0.0 SIGSEGV incident with no repeat at the snapshot.

All v0.7.3-native rows except one older-build upload match the exact production
UUID. None exposes a useful app frame after the CrashReporter handler, so there
is no evidence-backed audio, database, SwiftUI, or persistence patch to make.
App issue [#842](https://github.com/moona3k/macparakeet/issues/842) now tracks
native crash-log collection, normalized families, stable incident identity,
and prior-session attribution.

The website correction attributes crashes by payload version and crash time,
deduplicates a privacy-safe fingerprint, preserves the public JSON field for
compatibility, and applies the same contract to the 24-hour headline,
explicit-failure details, and deterministic reviewer. The reviewer also keeps
Sparkle `update` and unknown `legacy` traffic out of GUI counts and uses the
repository-pinned Wrangler binary.

Website pull request
[#30](https://github.com/moona3k/macparakeet-website/pull/30) merged as
`2692ac37ac1b37c2b1f3474e64fe6be2a6261b88` after all review findings were
resolved and the final Greptile check passed. Cloudflare Pages production
deployment `bfa6a077-6df6-4809-a756-5c3392969e44` was built from that merge.
The deployed API then reported rollup history through July 17 and 8 distinct
v0.7.3 incidents across 224 observed GUI sessions in its advancing 24-hour
window; the stats page labels the result as an estimate rather than claiming
8 exact crashing sessions.

The reusable backup gate also restored the full 795,071,800-byte pre-rollout
export, matched SHA-256
`c596c690c55beaf56c2dd2b09c4b345b09a1b216b0d365d6645f20f542767896`,
passed SQLite integrity, recovered 1,744,713 events and five indexes, and
confirmed the intended pre-rollup schema. File mode is `0600` on a
FileVault-encrypted Mac. Cloudflare Time Travel resolves the pre-migration
bookmark, but this Mac has no Time Machine destination; future deletion is
therefore blocked until a fresh encrypted export is stored and restored from
separately controlled off-machine media.

## Surface-by-surface results

| Surface | Snapshot result | Assessment |
|---|---:|---|
| Release artifact | GitHub release, appcast, and R2 asset all live; exact DMG was 148,896,323 bytes, SHA-256 `0c4ccb1b6f0cedae0f7994afce393094596aacdd8548af374ae18ae500ecdd8a` | Healthy |
| GUI ingestion | Latest rows arrived within seconds/minutes of inspection | Healthy |
| Crash health | 1/181 v0.7.3 sessions crashed (99.45% crash-free); 5 matching reports across v0.7.2-v0.7.3 | Watch, not a release regression |
| Dictation | 604 success, 9 true failure, 81 cancelled, 60 empty; true failure 1.47% of success + failure versus 2.63% on v0.7.2 in the same window | Healthy/improved |
| File transcription | 105 success, 0 failure, 3 cancelled | Healthy |
| YouTube transcription | 7 success across 6 sessions, 0 failure | Healthy, small sample |
| Diarization | 98 started, 95 completed, 3 failed/cancelled; transcription still completed | Healthy with cancellation noise |
| Meetings | 60 success, 1 `noAudioCaptured` failure, 17 permission-gated unavailable; 98.4% true success excluding permission gates | Healthy; permission friction remains |
| Meeting recovery | 2 discovered, 1 completed, 1 discarded, 0 failures | Healthy, small sample |
| Microphone-stall detector | 13 v0.7.3 sessions reported `mic_silent`; 5 lasted at least 30 seconds and 3 at least 2 minutes | Ongoing watch; not new in v0.7.3 |
| Models/downloads | 196 success, 3 failure; 75 downloads started and 69 completed | Healthy |
| Speech-engine switch | 23 success, 1 failure; 15 runtime-unhealthy breadcrumbs, mostly successful slow first setup | Healthy outcome, noisy watchdog taxonomy |
| LLM operations | 182 success, 40 failure, 3 unavailable; 18.0% true failure versus 18.3% on v0.7.2 | Existing provider-compatibility issue, not regression |
| Transforms | 13 success, 6 failures from one session, 10 no-provider unavailable | Watch, too concentrated/small to call systemic |
| Auto-save | 15/15 success | Healthy |
| Export/copy/history/snippets | Export 33, copy 23, history replay 10, snippet firing 21; no export failure signal | Healthy |
| Onboarding | 53 starters, 44 completers (83.0%); 18 same-session first dictations (40.9% of completers) | Completion healthy; activation remains an opportunity |
| Permissions | Microphone 44/45 grants; screen recording 14 denials from 16 prompts | Mic healthy; meeting first-use friction |
| Bundled CLI 3.0.0 | Help 112, meetings 20, export 12, transcribe 12, health 10; one LLM provider failure | Healthy |
| Homebrew CLI 2.14.0 | 403 transcription successes, 4 failures (99.0% success) | Healthy/watch independently of app release |
| Sparkle/update telemetry | Current checks are arriving; at least 62 v0.7.3 daily device hashes; 181/1,002 last-24-hour GUI sessions already on v0.7.3 | Healthy early adoption |
| GitHub download | 146 release-asset downloads at snapshot | Healthy; counter is not the same as installs |
| Website/Umami | Since release: 239 page views, 149 visitors, 167 visits, 42 download-click events | Healthy; click events are not exact downloads |
| Event contract | All 102 app event names are accepted by the Worker; Worker has only four extra legacy/planned names | Healthy |
| Opt-out/privacy path | 6 sessions emitted the opt-out breadcrumb; no content/audio observed in reviewed schema | Event path works |
| Raw-event retention | 62,943 rows older than 90 days; scheduled Worker not deployed | **Unhealthy** |
| Stats/dashboard | Cached response healthy; one cold HTTP 500; 5.788B D1 rows read in 24 hours | **Unhealthy cost/reliability profile** |
| Deterministic reviewer | Correctly says `WATCH`, but misclassifies NULL-surface Sparkle rows and duplicates GUI groups | Tooling defect |

Counts are live snapshots and naturally advanced during the audit. Operation
rates deliberately separate true `failure` from `cancelled`, `empty`, and
permission-gated `unavailable` outcomes, as required by the telemetry contract.

## Crash deep dive

The v0.7.3 report was emitted at `2026-07-17T10:48:50Z`, ten seconds after the
recorded crash time. That confirms the relaunch reporter path worked. It was a
SIGSEGV on an M4 Max running macOS 26.5.2.

The production DMG was downloaded again from the GitHub release rather than
using the stale local `dist` directory. Its executable UUID is
`2F269AAE-3409-321F-B387-0F2392D734E9`, exactly matching the crash payload. The
first frame resolves to the CrashReporter signal-handler closure, which is
expected for this custom signal report. On a matching macOS 26.5.2 host, the
next useful system frame maps into SQLite (`modifyPagePointer`), followed by
SwiftUI/CoreData frames and a SQLite locking tail.

After normalizing for ASLR, the entire 11-frame signature matches reports at:

- v0.7.2: July 14 at 09:32 and 09:33 UTC, July 16 at 20:25 UTC, and July 17 at
  07:20 UTC
- v0.7.3: July 17 at 10:48 UTC

This is enough to reject “one-off” as a signature-level conclusion, but not
enough to blame a specific app call site. There is no matching production dSYM
retained with the release, and the custom payload lacks Apple exception context,
thread state, and a reliably attributable pre-signal app frame. Open a tracking
issue keyed by the normalized signature and request Apple crash logs from
affected users before changing persistence or SwiftUI code.

## Telemetry infrastructure findings

### 1. The 90-day retention promise is not being met

The source tree defines `macparakeet-telemetry-retention`, scheduled daily at
02:00 UTC, and deletes `events` rows older than 90 days. A live
`wrangler deployments list --config wrangler.telemetry-retention.toml` check
returned that the Worker does not exist on the account. The oldest production
row was dated 2026-03-13, with 62,943 rows beyond the cutoff.

This is a privacy/governance issue before it is a capacity issue. The database
is about 950 MB, roughly 9.5% of D1's current 10 GB paid per-database maximum.
However, **do not deploy the raw delete in isolation**: `/api/stats` still
computes its All Time cards, cumulative/daily history, all-time speed, per-chip
speed, and dictation-destination mix directly from raw events.

At the follow-up snapshot, deleting the expired rows without rollups would have
removed 56,819 release-shaped GUI events from the public stats data set (3.8%),
about 1,872 currently counted distinct sessions (8.1%), 4.67 million words
(5.4%), 478 hours of transcription audio, and 146 hours of dictation audio. The
public history would begin on April 18 instead of the March 13 launch-data
boundary, shrink from 127 to 91 daily buckets, and lose one historical country.
The 24-hour and 30-day operational panels would remain intact, but the page's
“All Time” and “since launch” labels would no longer be truthful.

First materialize historical daily and cumulative rollups, switch the stats
endpoint to combine immutable pre-retention rollups with the live raw window,
and verify a before/after payload comparison. Then deploy the scheduled Worker,
confirm the old-row count reaches zero, and add an alert or deploy-time check so
source presence cannot be mistaken for production operation. This sequencing
also removes the main source of the stats endpoint's raw-scan cost. See
[Cloudflare's current D1 limits](https://developers.cloudflare.com/d1/platform/limits/).

### 2. Stats regeneration is expensive and occasionally brittle

D1's 24-hour counters at snapshot were:

- 26,974 read queries
- 35,076 write queries
- 5,788,330,111 rows read
- 256,434 rows written

The stats endpoint stores one aggregate snapshot for only five minutes. When it
expires, it rebuilds a large response using many raw-event aggregate queries.
At a sustained 5.788B rows/day, the projected 30-day total is 173.65B rows. Paid
D1 includes 25B rows/month and charges $0.001 per additional million rows, so
the illustrative excess is 148.65B rows, or about $148.65/month. Writes and
storage remain within the included paid allowances. See [Cloudflare's current
D1 pricing](https://developers.cloudflare.com/d1/platform/pricing/).

Replace repeated raw scans with incremental daily/hourly rollups, lengthen the
snapshot TTL where freshness permits, and make stale snapshot serving the
fallback when regeneration fails. Measure query-level rows-read before and
after. The single observed HTTP 500 is not enough to prove that scan volume was
its cause, but it makes this a reliability issue as well as a cost issue.

Follow-up status: historical daily rollups, a 15-minute snapshot with
stale-while-revalidate, and stale-on-error serving are deployed. Exact parity
is zero; ongoing rows-read and regeneration monitoring remains operational
work rather than a release blocker.

### 3. The reviewer conflated Sparkle and GUI traffic (resolved)

Sparkle checks intentionally have `surface IS NULL`. The deterministic reviewer
uses:

```sql
COALESCE(surface, CASE WHEN event = 'cli_operation' THEN 'cli' ELSE 'gui' END)
```

and then `GROUP BY surface`, where `surface` can bind to the underlying column
rather than the rendered alias. That produced a second apparent GUI group and
counted Sparkle device hashes as GUI sessions. PR #30 replaced it with explicit
GUI, CLI, `update`, and `legacy` classification, groups by expression position,
and adds a fixture covering all four surfaces. The deployed public stats path
continues to use strict GUI and CLI filters.

## Recommended order of work

1. **Completed: historical rollups and public cutover.** The additive schema,
   backfill, Worker, readiness fallback, stale snapshot fallback, and public
   endpoint are deployed. Exact raw-versus-rollup parity remains zero.
   Retention is deliberately not deployed, the delete-safe watermark is NULL,
   and no raw row has been deleted.
2. **Keep the deletion gate closed.** Before a separately approved retention
   rollout, require sustained rollup health, daily rows-read/error monitoring,
   a fresh Time Travel bookmark, and a full restore from a separately
   controlled encrypted off-machine export.
3. **Completed: create a crash-family issue, not an emergency patch.** App issue
   [#842](https://github.com/moona3k/macparakeet/issues/842) tracks the exact
   production UUID, normalized families, native Apple crash reports, and
   stronger client correlation. Re-evaluate at 500 and 1,000 v0.7.3 sessions
   or sooner if the rate rises.
4. **Completed: correct the telemetry reviewer and public crash estimate.** PR
   #30 separates GUI, CLI, Sparkle update, and legacy traffic; attributes and
   deduplicates crash payloads; and covers malformed timestamps, delayed
   uploads, and mismatched reporting builds with fixtures.
5. **Continue product-health follow-up without blocking rollout.** Validate the
   `mic_silent` detector against real audio gaps before Phase B recovery; split
   expected model optimization from genuinely stuck engine starts; keep LLM
   failures segmented by provider/version.

## Evidence and reproducibility

- Release truth: Git tag `v0.7.3` at
  `d6321f87dccecf29bd4792113f522bb0c98d1f35`, GitHub release API, live appcast,
  R2 headers, and the exact downloaded release DMG.
- App contract: `Sources/MacParakeetCore/Services/Telemetry/TelemetryEvent.swift`
  at fetched `origin/main` `c5e44eca0cb30d0a69670474ac10184104911b54`.
- Worker contract and stats implementation: sibling `macparakeet-website` at
  `1385cd247d67bb0e8e85fd36bad9175fcc528b0b`, especially
  `functions/api/telemetry.ts`, `functions/api/stats.ts`,
  `functions/_middleware.ts`, `scripts/telemetry-review.mjs`,
  `workers/telemetry-retention.ts`, and `wrangler.telemetry-retention.toml`.
- Live data: remote D1 database `macparakeet-telemetry`
  (`7372263e-6a0b-4c70-8188-8f1d6d16bf31`) queried with Wrangler. Queries were
  bounded by release timestamp and/or a 24-hour current window, then compared
  with v0.7.2 in the same window where relevant.
- Website analytics: live Umami API, bounded from the GitHub release publication
  timestamp. No token or visitor-level data is recorded in this report.
- Local symbol work: `hdiutil`, `dwarfdump --uuid`, `nm`, and `atos` against the
  exact production executable plus matching macOS 26.5.2 system images.
- Follow-up production proof: website PR #30, merge `2692ac3`, Pages deployment
  `bfa6a077-6df6-4809-a756-5c3392969e44`, 17 passing telemetry tests, an
  86-page production build, live public API/page checks, zero-delta rollup
  parity, and the full backup restore gate.

The initial observation phase made no production mutation. Follow-up changes
were additive rollups, cache/reviewer correctness, documentation, and the
public stats deployment described above. No telemetry row was deleted, the
retention Worker remains absent, and stale local `dist` output was never used
as release evidence.
